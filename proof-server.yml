services:
  proof-server:
    image: midnightntwrk/proof-server:8.0.3
    command: ["midnight-proof-server", "-v"]
    container_name: nightpass-proof-server
    ports:
      - "${NIGHTPASS_PROOF_PORT:-6300}:6300"
